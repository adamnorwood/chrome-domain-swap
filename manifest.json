{
    "manifest_version": 2,
    "name": "REAL/DEV Domain Swap",
    "description": "A Chrome extension that lets you swap between your real domain and a testing domain, handy for things like switching to a BrowserSync proxy",
    "version": "1.0",

    "background": {
        "scripts": ["chrome-domain-swap.js"]
    },

    "browser_action": {
        "default_icon": "icon.png",
        "default_title": "Swap between production and testing domains"
    },

    "permissions": [
        "activeTab",
        "storage"
    ]
}